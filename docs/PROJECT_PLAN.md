# 当前项目计划

最后更新：2026-08-26

## 当前阶段

首个参与者纵向里程碑和现场管理控制台已经实现并通过本地验收。当前代码包含活动届次、身份保护、版本化题库、抽题、服务端计时、幂等提交、分类最高分、题库管理命令、参与者 JSON 接口、正式参与者网页和自定义 `/ops/`。

V 盘 Ubuntu 24.04 预发布环境也已完成原生 PostgreSQL 18、Gunicorn、Caddy、systemd、提交态发布、回滚、备份恢复、重启持久性和安全拔盘预检演练。朋友服务器部署和公开发布仍未开始。

## 已确认产品规则

- 参与者提交显示名、学号或工号，以及手机号或邮箱二选一的联系方式。
- 显示名允许使用姓名或昵称；身份资料通过 HTTPS 传输并存入受控数据库。
- 每次从一个板块随机抽取 15 题，限时 5 分钟；题量和时间可配置，服务端校时。
- 允许重复挑战。每次记录独立保存，分类最高分用于管理员内部趣味榜。
- 结果页显示本次得分、分类最高分和每题对错，不直接公布正确答案。
- 内部趣味榜按板块和分类最高分降序排列，同分共享名次，不向参与者展示。
- 管理后台使用单一管理员角色，但每人使用独立账号并记录关键操作。
- 兑奖只维护少量可复用兑奖词及其显示条件，不作为安全凭证。

## 实施顺序

1. 建立参与者、身份资料、板块、题目、答题记录、分类最高分、兑奖词和管理员操作记录模型。
2. 实现 Excel 题库导入、来源与许可字段、稳定题号和失败即停止的校验。
3. 实现服务端抽题、计时、提交、原子计分和重复答题。
4. 建立并发与回归测试，目标覆盖 30 至 50 个同时提交。
5. 实现参与者页面和管理后台监控、检索、字段展示、导出及活动控制。
6. 基于已确认的角色主视觉展架方向完成参与者页面，并进行手机与桌面浏览器验收。
7. 在私有预发布环境验收，再制作可部署发布包。

## 当前实现进度

- 已完成领域模型、初始迁移、字段级身份加密、事件域查重摘要和密钥轮换命令。
- 已完成三表题库格式、校验、原子数据库导入、媒体复制、旧库转换与私密审核包。
- 已完成活动初始化、暂停后题库切换、服务端抽题、截止时间、幂等提交、最高分和作废重算。
- 已完成 `/api/v1/` 参与者接口、正式前端契约和 `/participant/` 页面。根路径重定向到参与者页面。
- 页面覆盖登记与再次进入、板块选择、选择题与填空题、题图、标签页草稿、恢复、暂停、关闭、超时、提交和结果。
- 页面只发布已批准的 Kano 像素 Yuriko sprite sheet；高清主视觉角色未进入正式静态资源。
- 已在 Docker Desktop 的 PostgreSQL 18 上通过五组并发门禁：不同参与者同时提交、同一答题重复提交、同编号登记、并发开始答题和最高分单调更新。
- 已通过 83 项 SQLite 快测、8 项 PostgreSQL 18 并发测试、Ruff、Django check、迁移漂移和 133 项静态资源收集检查。
- 已使用真实 Django Session、CSRF、PostgreSQL 和公开虚构题库完成 360×800、390×844、1440×900 Chromium 验收；控制台 0 error、0 warning。
- 已实现每人独立账号和单一权限的现场管理控制台。生产配置不暴露 Django 默认 `/admin/`。
- 已实现显式参与者入口、暂停或关闭后的恢复、监控快照、脱敏检索、页面级完整字段、两种流式 CSV、榜单、题库切换、兑奖词、作废和去身份化。
- 已增加 Ubuntu 24.04 原生 PostgreSQL 18、Gunicorn、Caddy、systemd、发布包、备份和恢复演练模板。
- 已在 `Ubuntu-Quiz-Lab` 完成提交 `328f204aa009` 的发布、上一版本回滚、WSL 终止重启、V/E 双份备份校验、全新数据库恢复和 50 路真实 HTTPS 提交。
- 已用 Linux Playwright 1.62.0 / Chromium 151 对 `/ops/` 完成 1440×900、390×844、360×800 验收；无页面横向溢出、无小于 44px 的可见触控目标，控制台和 page error 为 0。

## 外部依赖

- 朋友服务器的公网 80/443、隔离、备份和备案接入关系仍待确认。
- `mmdustc.top` 子域名名称和 DNS 操作仍待确认。
- 日协 GitHub Organization 尚未建立，当前不连接 remote。
- 展架视觉材料已经提供。像素 Yuriko 可复用；高清主视觉角色和其他素材仍需逐项完成网站发布审查。

## 首个里程碑完成条件

- PostgreSQL 业务模型和迁移可重复建立。
- 题库导入能拒绝重复题号、无效答案、缺失图片、板块题量不足和缺失溯源信息。
- 服务端保存抽题结果和截止时间，客户端不能篡改题号、板块或计时。
- 30 至 50 路同时提交不丢成绩，失败请求可观察且不会返回虚假成功。
- `python manage.py check`、`pytest` 和 `ruff check .` 通过。

参与者纵向里程碑、现场管理控制台与 V 盘 Linux 预发布演练均有本地验证证据。下一阶段是在不部署的前提下收集朋友服务器清单，并由用户决定是否批准 Windows 侧 WSL 单端口防火墙规则和 Caddy 根证书信任。
