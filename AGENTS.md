# 项目协作规则

## 读取入口

进行持续性或复杂工作前，若本机存在 `docs-agent/STATE.md`，先读取它，并只按其中链接加载当前 task 和参考资料。若该目录不存在，先读 `docs/PROJECT_PLAN.md`、`docs/research-handoff.md` 和相关 ADR。

## 权威边界

- 本仓库是程序、测试、公开文档和发布配置的唯一权威来源。
- 服务器只运行已验证发布版本，不作为源码编辑位置。
- `_private/` 保存现役题库、未审查素材、服务器本地资料和往届参考；它始终被 Git 忽略。
- `docs-agent/` 保存本机 Agent 运行状态；考虑到仓库计划公开，它始终被 Git 忽略。需要团队同步时另建私密伴随仓库。
- 公开仓库只允许示例题或已完成来源、许可和致谢审查的退役题。现役题库及答案不得进入 Git 历史。

## 实现顺序

1. 先保证数据库事务、服务端计时、题库导入和并发提交正确。
2. 再实现参与者流程、管理后台和现场操作。
3. 海报材料齐全后再调整前端视觉；不要用视觉改动掩盖未完成的数据边界。

旧部署包只作为 `_private/legacy-reference/` 中的行为与视觉参考。不要把旧代码整体复制为实现基线，也不要迁移历史参与者 CSV、旧密钥、缓存或临时模板。

## 隐私与安全

- 显示名、学号或工号和联系方式属于参与者身份资料。不得写入日志、测试夹具、截图文件名或 Git。
- 所有公开部署必须使用 HTTPS。密钥只从环境读取，不能写进代码、文档、镜像或发布包。
- 管理员账号一人一个，权限相同。导出、重置、标记无效和活动状态切换必须留痕。
- 修改忽略规则或准备公开仓库前，运行 `git status --short --ignored` 和 `git ls-files`，确认私密文件从未被跟踪。

## 验证

- Python 修改至少运行 `python manage.py check`、`pytest` 和 `ruff check .`。
- 数据模型、计分或题库修改还要覆盖并发提交、服务端超时、重复答题和分类最高分。
- UI 修改要走真实浏览器的手机与桌面流程。部署修改要运行 Django 部署检查，并验证 HTTPS、代理头、备份和恢复。
- 无法完成真实验证时，明确记录静态检查与实际验收之间的差距。

## 外部操作

连接 GitHub remote、推送、公开仓库、部署到服务器、修改 DNS 或处理真实参与者数据前，先确认目标和范围。网页登录、MFA、域名控制台和现场人工核验由人完成；Agent 先准备操作单和回传证据要求。

